---
import { getCollection } from "astro:content";
import { getSiteConfig, withBase } from "../../lib/site";
import { entrySlug, isLocaleEntry } from "../../lib/collections";
import { ui } from "../../data/ui";
import ArticleLayout from "../../layouts/ArticleLayout.astro";

const site = getSiteConfig(import.meta.env);
const copy = ui[site.locale];
const description = site.locale === "ro"
  ? "Articole de la Stories of Data."
  : "Articles from Stories of Data.";
const entries = (
  await getCollection("articles", (entry) => isLocaleEntry(entry.id, site.locale))
).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---
<ArticleLayout title={copy.articles} description={description} path="/articles" contentClass="work-index">
  <h1>{copy.articles}</h1>
  <ul>
    {entries.map((entry) => (
      <li>
        <a href={withBase(`/articles/${entrySlug(entry.id)}`, import.meta.env.BASE_URL)}>{entry.data.title}</a>
      </li>
    ))}
  </ul>
</ArticleLayout>
